import { SxProps, Theme } from '@mui/material';

export const couponFormStyles: {
  formBox: SxProps<Theme>;
  dialogContent: SxProps<Theme>;
} = {
  formBox: {
    mt: 2
  },
  dialogContent: {
    minHeight: '300px'
  }
}; 